# 前后端访问指南

## ✅ 当前状态

### 后端服务
- **状态**: ✅ 正在运行
- **端口**: 5001
- **进程ID**: 38260
- **API地址**: `http://localhost:5001`

### 前端服务
- **状态**: ✅ 正在运行
- **端口**: 3000
- **访问地址**: `http://localhost:3000`

## 🌐 访问方式

### 1. 前端页面（推荐）

直接在浏览器中打开：
```
http://localhost:3000
```

### 2. 使用标签筛选查看Meta DS岗位

在前端页面中：
1. 打开标签筛选器
2. 选择：
   - **Company**: Meta
   - **Category**: Data
3. 点击筛选，即可看到所有72条Meta DS岗位面经

### 3. 直接访问API

#### 获取所有Meta DS岗位：
```bash
curl 'http://localhost:5001/api/posts?company=Meta&category=Data&page=1&limit=20'
```

#### 获取筛选选项：
```bash
curl 'http://localhost:5001/api/filter-options'
```

#### 按时间排序：
```bash
curl 'http://localhost:5001/api/posts?company=Meta&category=Data&page=1&limit=20&sort=createdAt&order=desc'
```

## 📊 数据统计

- **总帖子数**: 796+ 条
- **Meta DS岗位**: 72 条
- **Meta所有岗位**: 508+ 条

## 🏷️ 支持的筛选维度

前端支持以下标签筛选：

1. **Company** (公司)
   - Meta, Google, Amazon 等

2. **Category** (部门类别)
   - SWE (软件工程)
   - Data (数据科学)
   - PM (产品经理)
   - Design (设计)
   - Infra (基础设施)
   - Other (其他)

3. **RecruitType** (招聘类型)
   - intern (实习)
   - newgrad (校招)
   - experienced (社招)

4. **Location** (地点)
   - 北京, 上海, San Francisco Bay Area 等

5. **Experience** (经验要求)
   - 0, 0-2, 2-5, 5-10, 10+

6. **Salary** (薪资范围)
   - 0-100k, 100k-150k, 150k-200k, 200k-300k, 300k+

7. **Technologies** (技术栈)
   - React, TypeScript, Python, SQL 等

## 🔍 示例查询

### 查看Meta DS岗位的最新面经：
```
http://localhost:3000
→ 筛选: Company=Meta, Category=Data
→ 排序: 按时间降序
```

### 查看包含SQL技术的Meta面经：
```
http://localhost:5001/api/posts?company=Meta&technologies=SQL&page=1&limit=20
```

### 查看校招岗位：
```
http://localhost:5001/api/posts?recruitType=newgrad&page=1&limit=20
```

## 🚀 快速测试

### 测试后端API：
```bash
# 测试基本API
curl http://localhost:5001/api/posts?page=1&limit=5

# 测试Meta DS筛选
curl 'http://localhost:5001/api/posts?company=Meta&category=Data&page=1&limit=3'
```

### 测试前端：
1. 打开浏览器
2. 访问: `http://localhost:3000`
3. 应该看到面经列表
4. 使用筛选器选择 Meta + Data

## 📝 数据特点

所有Meta DS岗位数据包含：
- ✅ 完整的 `tagDimensions` 结构
- ✅ `publishTime` 发布时间字段
- ✅ `createdAt` 创建时间字段
- ✅ 支持所有前端筛选维度
- ✅ 结构化内容（Markdown格式）

## 🔧 如果前端/后端未运行

### 启动后端：
```bash
cd /Users/henghuang/Desktop/代码/HH-main/backend
PORT=5001 node index.js
```

### 启动前端：
```bash
cd /Users/henghuang/Desktop/代码/HH-main/frontend
npm run dev
```

## ✨ 功能特性

1. **实时筛选**: 前端标签筛选器实时过滤数据
2. **分页加载**: 支持分页浏览大量数据
3. **时间排序**: 可按发布时间排序
4. **多维度筛选**: 支持多个标签维度组合筛选
5. **低延迟**: 数据已导入，查询响应快速

---

**现在可以打开 `http://localhost:3000` 查看所有面经数据了！** 🎉

