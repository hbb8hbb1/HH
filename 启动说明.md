# HH 项目启动指南

## 📁 项目结构

- **HH-main/** - 主项目目录（运行前后端需要）
  - 前端代码（React + TypeScript + Vite）
  - `server/` - 后端代码（Node.js + Express + MongoDB）

- **hh_pipeline/** - 数据处理流水线（可选，数据已导入后不需要）
  - 用于处理 HTML → JSON → 导入数据库

## 🚀 快速启动

### 1. 启动后端服务器

```bash
cd ~/Desktop/HH-main/server
PORT=5001 node index.js
```

**预期输出：**
```
MongoDB connected
Server running on port 5001
```

### 2. 启动前端开发服务器（新开一个终端窗口）

```bash
cd ~/Desktop/HH-main
npm run dev
```

**预期输出：**
```
VITE v6.4.1  ready in XXX ms
➜  Local:   http://localhost:3000/
```

### 3. 访问应用

- **前端页面**：http://localhost:3000
- **后端API**：http://localhost:5001/api/posts

## 📋 前置条件检查

### 检查后端依赖
```bash
cd ~/Desktop/HH-main/server
ls node_modules  # 如果为空，运行：npm install
```

### 检查前端依赖
```bash
cd ~/Desktop/HH-main
ls node_modules  # 如果为空，运行：npm install
```

### 检查 MongoDB 连接
确保 `.env` 文件中的 `MONGO_URI` 配置正确：
```bash
cd ~/Desktop/HH-main/server
cat .env
```

## 🔧 常见问题

### 端口被占用
```bash
# 查找占用端口的进程
lsof -i :5001
lsof -i :3000

# 结束进程（替换 PID 为实际进程号）
kill -9 PID
```

### MongoDB 连接失败
- 检查 MongoDB Atlas 网络访问设置
- 确认 IP 地址已添加到白名单
- 检查 `.env` 中的连接字符串

## 📝 数据导入（如果需要）

如果数据库为空，可以使用 `hh_pipeline` 导入数据：

```bash
cd ~/Desktop/hh_pipeline
python3 import_posts_via_api.py \
  --api-base http://127.0.0.1:5001 \
  --email "your@email.com" \
  --password "yourpassword" \
  --data-dir ./data_final
```

## ✅ 总结

**运行前后端只需要：HH-main 目录**

- 后端：`HH-main/server/`
- 前端：`HH-main/`（根目录）

hh_pipeline 是数据处理工具，数据已导入后不需要运行。

